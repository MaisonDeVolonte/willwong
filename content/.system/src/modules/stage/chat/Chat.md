# Chat

A lightweight chat UI component with streaming message support.

## Overview

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Messages stream in token by token and are rendered in a scrollable feed.

## Usage

Import `chat.tsx` and `chat.css`, mount `<Chat />` in your panel slot.

## Notes

- Message history is stored in local component state
- Streaming is handled via a ReadableStream from the API route
