# Panels

A resizable panel system built with vanilla JS and CSS custom properties.

## Overview

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Panels are draggable, collapsible, and responsive.

## Usage

Import `panels.tsx` and `panels.css`, mount `<Panels />` once in your layout.

## Notes

- Resize state is stored in CSS variables on `document.body`
- Panel groups collapse siblings automatically on mobile
