# docs/insights/2026-07-29.md

## Insight #1: 2026-07-29 10:04

### sources
sidecar: 5 broken mirrors, 0 markers | docs reconciled: README.md, AGENTS.md | logs: 2026-07-20
to 2026-07-29

### observations
- `content/AGENTS/{git,guides,logs,plans,prompts}.md` all mirror-point at `AGENTS/` paths that
  moved when the scaffold got extracted to the operator repo (07-23 log); the mirrors never
  followed
- AGENTS.md claims `docs/logs/`, `docs/prompts/`, `docs/audits/`, `docs/brutal/`, `docs/insights/`
  are gitignored runtime artifacts; `.gitignore` has no `docs/` entry, so all of it sits untracked
  instead of ignored
- the TaskCompleted hook reverting status to in_progress after every completion was flagged on
  07-21 and reproduced again on 07-21 Thread #3 (9+ times same session) — still open, never
  actioned at the hook level
- gitbrutal's own telemetry shows 3 test files against 72 app source files, consistent with the
  07-20/07-21 logs' focus on manual `evaluate`/`@retardify` passes over automated test-writing

### opportunities
**urgent and important:**
- fix the 5 broken `content/AGENTS/*.md` mirror pointers now that AGENTS/ moved to operator
- add `docs/logs/`, `docs/prompts/`, `docs/plans/`, `docs/study/`, `docs/audits/`, `docs/brutal/`,
  `docs/insights/` to `.gitignore` before anything gets accidentally staged

**urgent but not important:**
- resolve the 2 uncommitted tracked edits (`.claude/settings.json`, `webflow.json`) sitting
  unstaged on main

**not urgent but important:**
- fix the TaskCompleted hook double/over-firing bug, flagged twice on 07-21 and never patched
- raise test coverage: 3 test files for a 72-file app source tree is the thinnest lane in the repo
- restore the lost `@/`-alias and values-before-types rules into AGENTS.md, flagged 07-20 and
  still missing

**not urgent or important:**
- none surfaced this pass

### carried
n/a — first insights report on file
