# docs/insights/2026-08-05.md

## Insight #1: 2026-08-05 16:14

### sources
sidecar: 0 refs, 1 marker | docs: README, AGENTS.md | logs: 2026-07-29 to 2026-08-05

### observations
- the sidecar is clean, 0 broken mirrors, links, or see-tags across the new 61-pointer tree
- its 1 marker is the ci.yml markdown-skip TODO, open since the 07-29 log noted it
- `content/AGENTS` had drifted for a week, carrying 2 triggers operator had already renamed
- nothing detects that drift, `scripts/content.mjs:45` skips operator targets by design
- `scripts/mirrors.mjs`, the gate the 07-29 log proposed to catch exactly this, was never built
- README structure block lists 5 files in `src/cms/`, omitting `mirrors.ts` added 07-29
- README CMS section still says `content.generated.ts` bundles the `@mirror` files
- that is now only 45 of them, the other 61 resolve at request time
- the webhook busts `content-tag` and `stats-tag`, never `CACHE_MIRROR_TAG`
- an operator push therefore waits out the 60s revalidate rather than busting on demand
- README stack table pins Claude 3.5, Grok 4.5, Gemini 1.5, all behind the models in use
- vendor-lock Phase 2 is still unbuilt, and today's symlink question was the same question
- `tmp/` holds sidecar scratch, matched by no ignore rule, `.gitignore:54` only covers `.tmp/`
- operator's `README.md` trigger and template lists match its tree, no drift there

### opportunities
**urgent and important:**
- build `scripts/mirrors.mjs` as a CI gate, reconciling `content/AGENTS` against operator both ways
- fix the CMS section of `README.md`, its build-time claim misleads on how mirrors resolve now

**urgent but not important:**
- add `tmp/` to `.gitignore` beside `.tmp/`
- add `mirrors.ts` to the `src/cms/` structure block in `README.md`
- resolve or drop the markdown-skip TODO in `.github/workflows/ci.yml`, open since 07-29

**not urgent but important:**
- revisit Phase 2 of `docs/plans/2026-07-29-operation-vendor-lock.md`, asked again today
- bust `CACHE_MIRROR_TAG` in `src/app/api/webhooks/github/route.ts` so operator pushes land at once
- refresh the stack table model versions in `README.md`

**not urgent or important:**
- reconsider mirroring operator's 4 `settings.*.json` templates to a public page
- correct the `docs/prompts/` mislabel inside PR #176, carried from 07-29, needs a force-push

### carried
`scripts/mirrors.mjs` gate — carried 2 reports, and the drift it would catch happened meanwhile
vendor-lock Phase 2 — carried 2 reports, still deferred, still the answer to the team-access ask
