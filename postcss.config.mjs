/**
 * ========================================================================================
 * @file postcss.config.mjs - base postcss configuration
 * ========================================================================================
 * @description
 * - provides a bare-bones postcss config required by next.js for css processing
 * - currently maintains no active plugins
 * @see /next.config.ts/
 */

const config = {
  plugins: {},
};

export default config;
